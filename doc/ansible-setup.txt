Openstack-ansible setup instructions

install ansible (if openstack.cloud collection isn't included, install it separately with ansible-galaxy collection install openstack.cloud)

From openstack, download {project-name}-openrc.sh and clouds.yml (save this to /etc/openstack/clouds.yml or ~/.config/openstack/clouds.yml)

in terminal run: source {project-name}-openrc.sh


run ansible plays
